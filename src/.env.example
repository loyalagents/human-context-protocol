# Environment Configuration Template
# Copy this file to .env and customize the values

# Authentication Credentials
# Used by: Auth Service, Gateway, MCP Service
AUTH_USERNAME=admin
AUTH_PASSWORD=your-secure-password-here

# Domain Configuration
# For local development:
API_HOST=localhost
MCP_HOST=mcp.localhost
# For production:
# API_HOST=api.hcp.loyalagents.org
# MCP_HOST=mcp.hcp.loyalagents.org

# SSL/TLS Configuration (Required for cloud deployment)
LETSENCRYPT_EMAIL=your-email@example.com

# Security: MCP Fallback Routing
# Development: true (allows HTTPS testing via main domain)
# Production: false (MCP only accessible via dedicated subdomain)
ENABLE_MCP_FALLBACK=true

# GitHub Integration (Optional)
# Get token from: https://github.com/settings/tokens
# Scopes needed: repo, user
GITHUB_TOKEN=your-github-token-here

# Note: The .env file is ignored by git to keep your secrets safe
# Each developer should create their own .env file with their own values